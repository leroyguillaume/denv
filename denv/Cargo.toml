[package]
name = "denv"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
denv_lib = { path = "../denv_lib" }
exitcode = "1.1"
log = { version = "0.4", features = ["std"] }
